import { TruncateEllipsesPipe } from './truncate-ellipses.pipe';

describe('TruncateEllipsesPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateEllipsesPipe();
    expect(pipe).toBeTruthy();
  });
});
