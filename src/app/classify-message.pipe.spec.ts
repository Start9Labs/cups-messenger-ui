import { ClassifyMessagePipe } from './classify-message.pipe';

describe('ClassifyMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ClassifyMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
